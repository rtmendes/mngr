# mng-recursive

Recursive mng plugin.

A plugin for [mng](https://github.com/imbue-ai/mng) that injects mng configuration and dependencies into remote hosts during provisioning. This allows agents running on remote machines to use mng themselves.
