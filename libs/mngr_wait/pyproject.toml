[project]
name = "imbue-mngr-wait"
version = "0.1.3"
description = "Wait plugin for mngr - wait for agents/hosts to reach target states"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "imbue-mngr==0.2.6",
    "click-option-group>=0.5.6",
]

[project.entry-points.mngr]
wait = "imbue.mngr_wait.plugin"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["imbue"]

[tool.uv.sources]
imbue-mngr = { workspace = true }

[tool.pyright]
venvPath = "../.."
venv = ".venv"
pythonVersion = "3.12"
strict = ["**/*.py"]

[tool.pytest.ini_options]
timeout_func_only = true
pythonpath = ["."]
testpaths = ["."]
addopts = [
    "--cov=imbue.mngr_wait",
    "--cov-report=term-missing",
    "--timeout=30",
]

[tool.coverage.run]
source = ["imbue.mngr_wait"]
omit = [
    "*_test.py",
    "test_*.py",
    "*/conftest.py",
]

[tool.coverage.report]
# Lowered with ~5pt headroom because cli.py's coverage oscillates
# between runs (real test nondeterminism; tests pass deterministically
# but exercise different branches). Raise once the underlying test
# flakes are eliminated.
fail_under = 75
exclude_lines = [
    "assert_never",
]
