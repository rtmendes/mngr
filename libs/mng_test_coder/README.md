# mng-test-coder

Test agent type plugin for mng.

A plugin for [mng](https://github.com/imbue-ai/mng) that provides the test-coder agent type -- a mind agent that uses the matched-responses model for testing without API keys. Useful for integration and acceptance tests.
