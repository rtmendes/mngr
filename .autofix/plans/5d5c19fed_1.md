# Issue 1: Change-process comment in cli/create.py

## What and where
In `libs/mngr/imbue/mngr/cli/create.py` at lines 1056-1058, the comment reads:

```python
# For worktree mode without an explicit new branch (e.g. --branch foo with no colon),
# the branch is treated as an existing branch to check out in the worktree.
# No error needed -- git worktree add supports checking out existing branches.
```

## Why it is a problem
The third line ("No error needed -- ...") is a change-process artifact. It explains why a previous validation was removed rather than describing current behavior. Per the `user_request_artifacts_left_in_code` category, comments should describe what the code does, not how it was changed.

## Planned fix
Remove the third comment line ("No error needed -- ..."). The first two lines already describe the current behavior clearly. Alternatively, the remaining comment could be simplified since the code path is now self-explanatory.

## Risks
Minimal -- this is a comment-only change.
